# Stix — blockers

Format (template_dos_port canon): an entry the moment a slice is reverted —
`### YYYY-MM-DD — <symptom>` with Where / Evidence / Attempts / Hypothesis /
Likely unblocks when. A logged blocker is progress; a workaround is debt.

## Open

(none)

## Resolved

(none — bring-up completed without reverted slices)
