# Normalize line endings to LF in the repo (checked out LF everywhere).
* text=auto eol=lf

# Binary assets never get line-ending translation.
*.png binary
*.d64 binary
*.c64snap binary
*.tick binary
